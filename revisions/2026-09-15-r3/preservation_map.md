# R3 preservation map

- `ECTA.tex`, `supp.tex`, historical `.dat` arrays, figures, and prior review folders remain unchanged for provenance.
- `ECTA_R2.tex` is retained as the source identity and revised in place for R3; the R3 manifest records its final digest.
- `SUPP_R2.tex` remains the standalone supplement and uses the same temporal-self equations as the main source.
- R1 and R2 referee reports and manifests are copied into the R3 branch under `reviews/` so the audit inputs are immutable and local.
- The new executable reference harness does not overwrite historical arrays and never treats them as current evidence.
