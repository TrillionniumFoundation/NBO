# Development and execution disclosure

The revision was developed from the pinned R9 source artifact at `755b507870b54a100616b0658eba9a031eef2833`, while the remote branch preserves the latest review parent `30548ad06852cc0a447dedf1e63cc9e7f79f6c06`. The intervening commits add review packages, not new scientific manuscripts.

Exploratory computations selected the explicitly reported five-dimensional box. No claim that this is the maximal region is made. The theoretical certificate, not favorable sample signs, establishes the final region. Original adverse corners and permission interventions are retained and reoptimized.

A first integrated runner failed its joint stress assertion because its initial first-date search considered stored knots but omitted an arbitrary cap endpoint. It was not accepted as a successful run. The corrected search uses affine interpolation between the bracketing stored rows at every cap; common certificate endpoints are already explicit knots. First-date selected-control moment reconstruction checks the correction at newly drawn interior caps. The final runner has no resume mode and was executed anew after the fix.

The final run also deposits first-date operator rows, explicitly checks the initial frozen-proposal sign, and fixes thread settings before NumPy import. These changes were followed by another fresh complete run. Local LaTeX compilation and MuPDF inspection covered the revised front matter, central theory pages, and added supplement. GitHub reruns the science and build from the deposited source package and commits the resulting manuscript/evidence files; source packaging alone is not its success condition.

Historical neural training, all older numerical sweeps, and every inherited extension were not independently rerun. The new computations use inherited reward/transition construction and frozen proposals. Selected-control reconstruction is independent of the CSR backup, but is not an independent construction of the economic primitives. Array evaluation error, constructor error, and diffusion error remain distinct.
