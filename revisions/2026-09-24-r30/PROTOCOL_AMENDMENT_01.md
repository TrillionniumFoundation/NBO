# R30 amendment 01 — September 24, 2026

This amendment is after the first local diagnostic execution. It does not alter the frozen three economic gain functions, 60 neural candidates, five seeds, two widths, two optimizers, original-economy primitives, restart, or five constant-adjustment values. No raw candidate is removed or retuned.

## Validated stopping implementation
The first nested complex-ball integral failed to produce a finite enclosure at theta=0. Its source, output and failure log are retained under results/stopping/attempts/. The replacement uses the same killed-kernel/Dynkin identity, evaluates the spatial integral through truncated Gaussian moment recurrences, and then performs one-dimensional validated integration in sqrt(time). A degree-128 utility polynomial has a uniform remainder bounded by (16/9)*(4/5)^129/(1/5). The time interval [0,10^-6] and the opposite boundary have separate analytic likelihood-score bounds. Successful payoff/first/second derivative enclosures must have radius below .005; an unresolved computation is not a certificate. This is an implementation change, not a change of economic instance.

## Stronger classical controls
The structural direct-solve comparison is augmented by the exact symbolic sign rule a(x)=1{f(x)>=0} and an online exact budget guard. The former has zero regret in the exogenous-transition economy. The latter retains the incumbent exactly when its local deficit does not exceed eta and therefore attains the minimum nonnegative weighted intervention cost in the local-loss class. These controls need no state enumeration. They are important limitations on any neural-specific efficiency claim, not weak baselines selected to favor the neural cover. Their proof, query implementation and tests are retained alongside the Bernstein-based structural solver and vectorized DP.

The inherited central-state scalar oracle is also compared with derivative bracketing. Both search procedures use the same verified oracle and concavity audit; achieved projected-gradient bounds, calls, setup cost and search cost are all reported. The inherited polling schedule and the bracketing target are shown explicitly rather than treated as identical achieved bounds.

## Interpretation
The local initial execution found 26/60 raw neural candidates meeting the uniform .01 criterion in the new operating-mode family. This is not a claim about the inherited inventory failures, a high-dimensional neural advantage, or 60 independent economic models. The active-boundary probability at theta=0 is enclosed near .6891565168; second derivative signs differ across tested controls. Five pointwise derivative signs are not a proof of interval-wide monotonicity.

All timing evidence is single-environment descriptive evidence, not a runtime confidence interval. The illustrative intervention shadow price 1/20 is not a preregistered monetary estimate; break-even shadow prices are disclosed. The original current-state all-domain .01 target and the 47-dimensional derivative/residual bridge remain distinct numerical requirements.
