\section{New execution and proof-dependency record}\label{supp:r22dependencies}
The prospective crossed experiment is fixed in commit \path{082a3fdcf97d426df65858702b506bcdd7384ee3}. The separate full-state refinement is fixed in \path{a1e73f4262a232db147b245521af6af429e8f501}. The root and recovery diagnostics are fixed after observing the primary outcomes, in \path{98df6e83a2d0ccfae0aab65d5750344e8b629414}. Pair-moment analysis is a posteriori certification of the complete frozen experiment. Historical R20 and R21 inputs are not retrained or overwritten.

\begin{table}[htbp]
\caption{Proof-critical dependencies and diagnostic checks}\label{tab:r22dependencies}
\small\begin{tabularx}{\textwidth}{p{.19\textwidth}XX}
\toprule
Claim & Analytic and executable dependency & Result and status\\\midrule
Expert payoff and finance & Original decoder and stopped Gaussian reduction; R20 \path{certify_stochastic.py:certify} & \path{results/crossed/*/*/*/candidate_certificate.json}; directed binary64, proof-critical.\\
Unequal-initialization Jensen upper & Main Theorem~\ref{main-thm:r22jensen}; absolute Hessian cover; \path{moment_jensen.py:pair_moments} and \path{moment_jensen} & \path{results/moments/}; directed Cauchy/Taylor quadrature and Gaussian tails, proof-critical.\\
Uniform payoff and regret & Fixed nonnegative mixture weights, common-event allowance, convex unrestricted upper; \path{moment_jensen.py:main} & \path{results/moment_continuum.json}; outward endpoint algebra, proof-critical.\\
Range-based cross-check & Main Appendix~\ref{main-app:r22proofs}; \path{continuum.py:general_jensen} & \path{results/continuum.json}; separate conservative analytic bound, not independent economic mathematics.\\
Complex quadrature constants & Main Appendix~\ref{main-app:r22quadrature}; \path{proof_checks.py} & \path{results/proof_checks.json}; interval inequalities, proof-critical constants.\\
Full-state global witness & Accessible-face trace theorem and continuous-action oracle; R16 \path{accessibility_certificate.py:audit} & \path{results/full_state/}; MPFR, 1,024 cells, zero skipped cells, proof-critical.\\
Every starting time & Main equation~\ref{main-eq:r22suffix}; \path{time_envelope.py} & \path{results/full_state/time_envelope.json}; directed MPFR recursion, proof-critical.\\
Matched initialization and counts & \path{crossed.py}; exact delivered-policy equality & \path{matching.json} and \path{record.json}; execution invariants, not a value theorem.\\
Rollback and recovery & \path{crossed.py:run_stress}; \path{repair.py:rejection_recovery} & \path{results/stress/} and \path{results/rejection_recovery/}; state integrity and finite execution evidence.\\
Current-state dependence & Actor derivatives at four specified states & \path{state_dependence_diagnostic.json}; diagnostic only, not a coverage proof.\\
Online cost & Main Proposition~\ref{main-prop:r22cover}; \path{runtime_cost.py} & \path{results/runtime_cost.json}; ordinary-float timing only, no online error certificate.\\
\bottomrule
\end{tabularx}
\end{table}

The binary64 and MPFR paths share economic assumptions and, where reused, the same stopping and quadrature mathematics. Arithmetic redundancy does not establish mathematical independence. The new pair-moment theorem is proved in the main appendix; an arithmetic overlap or a state-hash check cannot replace it.

\section{Complete configuration-level accounting}
Each row below is one final vertex proposal, including the rejected financing proposal. ``Delivered regret'' is the certified regret of the delivered policy, which is the initial incumbent when a proposal is rejected. Function and gradient counts are equal in this design; L-BFGS-B line-search counts mean calls beyond its initial call and one call per accepted iterate. There is one shared initial check per block and one final check per configuration. Generation and checker clocks exclude interpreter startup, imports, installation, and the historical dual construction; those exclusions are explicit rather than silently treated as free end-to-end work.
\input{revisions/2026-09-23-r22/paper/full_ledger}

\section{Post-observation repairs and runtime measurements}
The four root-safe diagnostic configurations are all reported below. They concern only the selected failed block and are not a new confirmatory four-vertex ensemble. Re-budgeting frozen raw parameters changes the delivered financing offset; it is not an unchanged-policy welfare calculation.
\input{revisions/2026-09-23-r22/paper/repair_table}
\input{revisions/2026-09-23-r22/paper/runtime_table}
The runtime experiment duplicates coefficient arrays to vary the number of priced experts. It does not train additional independent experts, certify a larger domain, or establish learned state generalization. Reserve midpoints and ordinary quadrature are used for timing; the mathematical deployment theorem concerns the exact price-defined reserve and hedge.

\section{Review scope and preserved material}
The complete R21 final report is retained at \path{reviews/2026-09-23-econometrica-r21-final/referee_report.md}. The response addresses all twelve scientific findings and all seven technical comments individually. The new current-state certificate, local optimizer-conditional advantage, and general Jensen theorem are distinguished from the still unmet global $.01$ goal and unrestricted neural frontier claim. No certification failure is relabeled as a successful proposal.

All earlier scientific sources and numerical outputs remain in place. The R21 entry index is copied byte-for-byte to the revision archive before the root index is updated. Earlier introduction and conclusion text is reproduced below as historical exposition, so the revised main narrative does not delete it.
