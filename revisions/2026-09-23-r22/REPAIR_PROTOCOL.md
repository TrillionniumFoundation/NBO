# R22 post-observation repair protocol

The frozen primary 48-configuration experiment is complete and is not overwritten. Its independently initialized neural-Adam groups certify strictly positive continuum improvement. One neural L-BFGS-B proposal (base seed22000, vertex1) fails the exact reserve check despite numerical optimizer convergence: the original fixed budget-root bracket [-10,10] does not bracket that raw network's root. The original fixed-step stress run accepts3 and rejects5; exact rollback then deterministically repeats the same rejected proposal. Both adverse observations must remain in the paper and results.

Before follow-up execution, freeze the following two explicitly exploratory repair tests.

1. Root safety: use the same economic proposal objective with only its budget-root implementation changed. Center raw intercepts by their slab mean, expand the bisection bracket geometrically until the proposal-quadrature budget is enclosed, then perform80 bisections and the original implicit-gradient correction. Do not change the independent directed checker. Check common-intercept-shift invariance and successful bracketing against the failed raw network. Rerun the four optimizer/representation configurations for base seed22000, vertex1 only, with the exact same delivered initial policy matching and the original400 gradient-call cap. Record these as post-observation diagnostic repairs, not replacements or confirmatory primary endpoints. Keep the independently checked raw failed candidate and the repaired fixed-parameter candidate.

2. Rejection recovery: seed22300, first vertex, Adam starting learning rate0.5, eight blocks of25 gradient calls as before. Use the root-safe objective. Restore network, optimizer, and RNG exactly immediately after a rejected block and verify hashes. After verifying restoration, halve the learning rate in a separately recorded supervisory controller; this controller state is not rolled back. Report every rejected and accepted block, controller changes, gradients, and checker costs. Do not count deliberately injected faults as naturally rejected optimizer proposals. This tests escape from deterministic rollback repetition, not broad robustness or convergence of the solver.
