# R22 full-state continuation-witness experiment

This is an exploratory warm-start experiment, separate from the prospective local crossed experiment and not a claim of full-domain 0.01 closure. Freeze these choices before execution: original k=2 stopped economy; current-state input (t,u,x); accessible-face-softplus witness and wealth-distance portfolio architecture; inherited R17 seed17107 checkpoint400 as the explicitly charged warm start; 32 hidden units; joint actor/witness L-BFGS-B, maximum 400 objective/gradient calls, maxls=40, ftol=1e-13, gtol=1e-8. Optimize the unchanged R17 complete-box differentiable interval surrogate, grid (4,16,16), temperature0.15. Retain the initial model, the last accepted L-BFGS iterate at budget exhaustion, counters, and raw certificates. Neither a sampled residual nor a failed line-search point is a certificate. Independently replay the initial and final models with the original MPFR complete-domain checker on (4,16,16), output perturbation2^-24, zero skipped cells. Check actual derivatives of the actor with respect to current u and x at declared diagnostic states; these are diagnostics, not coverage certificates. A decrease of this witness-based upper bound is not claimed to prove an increase of the policy's actual payoff.

As a separate constructive witness analysis, investigate state-dependent residual-correction barriers. Any proposed improvement must pass the whole-domain directed check and the accessible stopping-face trace conditions. Failed candidates are retained. Classical or analytic witness components must not be labeled neural outputs. Any unresolved full-state policy-value separation, continuation trace, or 0.01 global target remains explicitly unresolved rather than being replaced by the local K bound.
